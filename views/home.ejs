<%- include('partials/hero') %>

<%# 3.2 — Social proof strip %>
<section class="social-proof">
  <div class="shell">
    <span class="social-proof__eyebrow"><%= page.socialProof.eyebrow %></span>

    <%# Auto-scrolling marquee: the track holds the chip list twice back-to-back
        and animates to -50%, so the moment it resets it's showing an identical
        copy of what was already there -- looks like an unbroken loop instead of
        a visible jump. The second copy is aria-hidden so screen readers only
        ever hear the list once; see .social-proof__track / @keyframes marquee
        in styles.css for the reduced-motion fallback (plain manual scrolling,
        same as before this change). %>
    <div class="social-proof__chips">
      <div class="social-proof__track">
        <div class="social-proof__group">
          <% page.socialProof.verticals.forEach(function (v) { %>
            <span class="social-proof__chip"><%= v %></span>
          <% }); %>
        </div>
        <div class="social-proof__group" aria-hidden="true">
          <% page.socialProof.verticals.forEach(function (v) { %>
            <span class="social-proof__chip"><%= v %></span>
          <% }); %>
        </div>
      </div>
    </div>
    <p class="social-proof__markets"><%= page.socialProof.markets %></p>
  </div>
</section>


<%# Full-bleed photo banner %>
<section class="banner-photo">
  <img class="banner-photo__img" src="/img/photos/banner-team.jpg" alt="" aria-hidden="true" width="1920" height="1071">
  <span class="banner-photo__scrim" aria-hidden="true"></span>
  <div class="shell banner-photo__inner">
    <h2 class="banner-photo__headline"><%= page.banner.headline %></h2>
  </div>
</section>

<%# 3.3 — Divisions, alternating full-photo + bullet-list rows. Same five
    divisions (and the same live/roadmap facts) as What We Do's divisions
    section -- both pages read from the shared `divisions` local (see
    server.js), so this list can't drift out of sync with that page. %>
<section class="home-section" id="services">
  <div class="shell home-section__inner">
    <span class="eyebrow"><%= page.services.eyebrow %></span>
    <h2 class="home-section__heading"><%= page.services.headline %></h2>
    <p class="home-section__body"><%= page.services.body %></p>

    <div class="service-rows">
      <% var divisionPhotos = {
        Operations: '/img/photos/service-core.jpg',
        Analytics: '/img/photos/service-pulse.jpg',
        Digital: '/img/photos/service-link.jpg',
        AI: '/img/photos/service-signal.jpg',
        Advisory: '/img/photos/service-desk.jpg'
      }; %>
      <% divisions.forEach(function (d, i) { %>
        <article class="service-row <%= i % 2 === 1 ? 'service-row--reverse' : '' %>">
          <div class="service-row__media <%= i % 2 === 1 ? 'reveal-right' : 'reveal-left' %>">
            <img src="<%= divisionPhotos[d.name] || '/img/photos/service-core.jpg' %>" alt="<%= d.name %>" width="1024" height="768">
          </div>
          <div class="service-row__copy">
            <h3 class="service-row__name">
              <%= d.name %>
              <%- include('partials/badge', { status: d.status }) %>
            </h3>
            <p class="home-section__body" style="margin-bottom:1.2rem;"><%= d.summary %></p>
            <ul class="service-row__list">
              <% d.capabilities.forEach(function (c) { %>
                <li><%= c %></li>
              <% }); %>
            </ul>
            <a class="btn btn--ghost btn--pill" href="#contact">Talk to us →</a>
          </div>
        </article>
      <% }); %>
    </div>
  </div>
</section>

<%# 3.4 — Three entry-point tiers, numbered 01/02/03 %>
<section class="home-section" id="tiers">
  <div class="shell home-section__inner">
    <span class="eyebrow"><%= page.tiers.eyebrow %></span>
    <h2 class="home-section__heading"><%= page.tiers.headline %></h2>

    <div class="tier-cards">
      <% page.tiers.items.forEach(function (tier) { %>
        <article class="tier-card">
          <span class="tier-card__label"><%= tier.label %></span>
          <h3 class="tier-card__name"><%= tier.name %></h3>
          <p class="tier-card__body"><%= tier.body %></p>
          <a class="tier-card__link" href="<%= tier.link.href %>"><%= tier.link.label %> →</a>
          <% if (tier.note) { %>
            <p class="tier-card__note"><%= tier.note %></p>
          <% } %>
        </article>
      <% }); %>
    </div>
  </div>
</section>

<%# 3.5 — How it works, three steps (dark) %>
<section class="section--home-dark" id="how">
  <div class="shell home-section__inner">
    <span class="eyebrow"><%= page.how.eyebrow %></span>
    <h2 class="home-section__heading"><%= page.how.headline %></h2>

    <%# Vertical connected-spine timeline: one line icon per step (Scope =
        find/magnifier, Onboard = plug in, Run = execute/play), each marker
        pulsing continuously and independently -- reads as "always running,"
        not a one-off load animation. Icons are matched to step position
        (0/1/2), not looked up by title, since these three steps are a fixed
        narrative, not an open-ended repeatable list. %>
    <div class="how-spine">
      <% page.how.steps.forEach(function (step, i) { %>
        <div class="how-spine__step">
          <div class="how-spine__marker" aria-hidden="true">
            <div class="how-spine__ring">
              <% if (i === 0) { %>
                <svg viewBox="0 0 24 24"><circle cx="10.5" cy="10.5" r="6.5"/><line x1="15.4" y1="15.4" x2="21" y2="21"/></svg>
              <% } else if (i === 1) { %>
                <svg viewBox="0 0 24 24"><path d="M9 3v4M15 3v4M6 7h12l-1 5a5 5 0 0 1-10 0L6 7Z"/><path d="M12 16v5"/></svg>
              <% } else { %>
                <svg viewBox="0 0 24 24"><polygon points="8,5 19,12 8,19"/></svg>
              <% } %>
            </div>
          </div>
          <h3 class="how-step__title"><%= step.title %></h3>
          <p class="how-step__body"><%= step.body %></p>
        </div>
      <% }); %>
    </div>
  </div>
</section>

<%# 3.7 — Editorial quote block (dark) %>
<section class="quote-block">
  <div class="shell quote-block__inner">
    <h3 class="quote-block__headline">&ldquo;<%= page.quote.headline %>&rdquo;</h3>
    <p class="quote-block__body"><%= page.quote.body %></p>
  </div>
</section>


<%# 3.9 — FAQ accordion %>
<section class="home-section" id="faq">
  <div class="shell home-section__inner">
    <span class="eyebrow"><%= page.faq.eyebrow %></span>
    <h2 class="home-section__heading"><%= page.faq.headline %></h2>

    <div class="faq-list">
      <% page.faq.items.forEach(function (item, i) { %>
        <div class="faq-item">
          <button type="button" class="faq-item__trigger" aria-expanded="false" aria-controls="faq-panel-<%= i %>">
            <span><%= item.question %></span>
            <span class="faq-item__icon" aria-hidden="true"></span>
          </button>
          <div class="faq-item__panel" id="faq-panel-<%= i %>" aria-hidden="true">
            <div class="faq-item__panel-inner"><%= item.answer %></div>
          </div>
        </div>
      <% }); %>
    </div>
  </div>
</section>

<%# 3.10 — Contact CTA %>
<section class="home-section contact-cta" id="contact">
  <div class="shell home-section__inner">
    <h2 class="home-section__heading"><%= page.contactCta.headline %></h2>
    <p class="contact-cta__body"><%= page.contactCta.body %></p>

    <div class="contact-cta__actions">
      <a class="btn btn--primary btn--pill" href="mailto:<%= site.contactEmail %>"><%= site.contactEmail %></a>
      <a class="contact-cta__secondary" href="<%= page.contactCta.bookingHref %>"><%= page.contactCta.bookingNote %></a>
    </div>
  </div>
</section>
