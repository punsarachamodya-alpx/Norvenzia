{
  "name": "massifyx-global",
  "version": "1.0.0",
  "private": true,
  "description": "MassifyX Global — procurement & supply chain operations",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "test": "node --test test/*.test.js",
    "test:sweden-trade": "node --test scripts/sweden-trade/*.test.js",
    "data:sweden-trade": "node scripts/sweden-trade/build.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "ejs": "^6.0.1",
    "express": "^5.1.0",
    "express-ejs-layouts": "^2.5.1",
    "express-rate-limit": "^8.6.2",
    "express-session": "^1.19.0",
    "helmet": "^8.1.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.3"
  }
}
